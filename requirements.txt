# ── Core Bot ────────────────────────────────────────────────────────────────
python-telegram-bot==20.7          # Async Telegram Bot API wrapper

# ── NLP / Date Parsing ──────────────────────────────────────────────────────
dateparser==1.2.0                  # Multi-language natural date extraction (Arabic + English)
pyarabic==0.6.15                   # Arabic text normalization utilities

# ── Google APIs ─────────────────────────────────────────────────────────────
google-auth==2.27.0                # Google OAuth2 credential management
google-auth-oauthlib==1.2.0        # OAuth 2.0 flow helper
google-auth-httplib2==0.2.0        # HTTP transport adapter
google-api-python-client==2.116.0  # Google Tasks & Calendar REST clients
httpx==0.27.0                      # Async HTTP client for direct API calls

# ── Config ───────────────────────────────────────────────────────────────────
pydantic-settings==2.2.1           # Type-safe .env configuration

# ── Dev / Testing ────────────────────────────────────────────────────────────
pytest==8.0.0
pytest-asyncio==0.23.5
